# Base URL of the backend API (no /api suffix — the app adds it per request).
# When unset, the app guesses the Expo dev machine's IP on port 5000, which
# works for development with `expo start`. Set this for production builds.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:5000
